<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	android:installLocation="auto"
	package="net.yumnumm.KyoshinEewViewer">
	<uses-permission android:name="android.permission.INTERNET" />
	<application android:label="KEVi" android:icon="@drawable/icon" android:usesCleartextTraffic="true" />
	<!-- targetSdkVersion は SDK の既定に任せず明示的に固定する。
	     未指定だと 28 相当に落ち、SDK の更新で既定が動くと Scoped Storage の適用有無など
	     ストレージ周りの挙動が変わってしまうため。
	     値はコンパイル対象 (csproj の TargetFramework = net10.0-android、Android API 36) と揃える。
	     これより大きい値は指定できないため、API を上げるときは TargetFramework と一緒に上げること。
	     なお API 35 以降を対象にするとシステムバーの裏まで描画する edge-to-edge が強制される。
	     minSdkVersion は csproj の SupportedOSPlatformVersion から生成されるのでここには書かない -->
	<uses-sdk android:targetSdkVersion="36" />
</manifest>
